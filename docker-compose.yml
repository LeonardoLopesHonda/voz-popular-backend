version: '1.0'
services:
  postgres:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres123
    volumes:
      - postgres:/var/lib/postgresql
    ports:
      - '5432:5432'

volumes:
  postgres: